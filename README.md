# 🏸 羽球排點系統

智能化羽球比賽配對系統，根據球員等級自動匹配合適的對手。

## 專案結構

```
badminton-arrange-points/
├── frontend/          # Next.js + React 前端應用
│   ├── src/
│   │   ├── app/      # Next.js App Router
│   │   ├── components/
│   │   ├── contexts/
│   │   ├── hooks/
│   │   ├── lib/
│   │   ├── types/
│   │   └── styles/
│   └── package.json
├── backend/           # Node.js + Express 後端 API
│   ├── src/
│   │   ├── config/
│   │   ├── controllers/
│   │   ├── middleware/
│   │   ├── routes/
│   │   ├── services/
│   │   ├── types/
│   │   ├── utils/
│   │   └── jobs/
│   ├── prisma/
│   │   └── schema.prisma
│   └── package.json
└── README.md
```

## 技術棧

### 前端
- **框架**: Next.js 15+ (React 18+)
- **語言**: TypeScript
- **UI**: Material-UI (MUI)
- **狀態管理**: React Context API
- **表單**: React Hook Form + Zod
- **HTTP 客戶端**: Axios

### 後端
- **框架**: Express.js
- **語言**: TypeScript
- **ORM**: Prisma
- **數據庫**: PostgreSQL
- **認證**: JWT
- **日誌**: Winston

## 前置需求

- Node.js 18+ 和 npm 或 yarn
- PostgreSQL 12+（可選，開發時可用 SQLite）

## 安裝與運行

### 1. 克隆專案

```bash
git clone https://github.com/Jacky20000326/badminton-arrange-points.git
cd badminton-arrange-points
```

### 2. 安裝依賴

#### 後端
```bash
cd backend
npm install
```

#### 前端
```bash
cd ../frontend
npm install
```

### 3. 配置數據庫

#### 使用 SQLite（開發）
`.env` 已配置為使用 SQLite，無需額外設置。

#### 使用 PostgreSQL（推薦生產）
1. 建立 PostgreSQL 數據庫
2. 更新 `backend/.env` 中的 `DATABASE_URL`

```bash
DATABASE_URL="postgresql://username:password@localhost:5432/badminton_db"
```

### 4. 運行遷移

```bash
cd backend
npm run db:push  # 使用 Prisma push
```

### 5. 啟動開發服務器

#### 後端 (在 `backend/` 目錄)
```bash
npm run dev
```
服務器運行在 `http://localhost:3001`

#### 前端 (在 `frontend/` 目錄)
```bash
npm run dev
```
應用運行在 `http://localhost:3000`

## API 文檔

### 健康檢查
```
GET /health
```

### 認證 API
```
POST /api/auth/register        # 註冊新用戶
POST /api/auth/login           # 登入
POST /api/auth/logout          # 登出
GET  /api/auth/me              # 獲取當前用戶
PUT  /api/auth/profile         # 更新個人資料
```

（其他 API 端點將在後續階段實現）

## 開發計劃

### Phase 1: 專案初始化 ✅
- [x] 初始化 Next.js 前端專案
- [x] 初始化 Express 後端專案
- [x] 配置 TypeScript
- [x] 設置 Prisma 和數據庫 Schema
- [x] 建立基礎文件結構

### Phase 2: 認證系統 (進行中)
- [ ] 實現用戶註冊 API
- [ ] 實現登入 API（JWT）
- [ ] 實現認證中間件
- [ ] 建立前端登入/註冊頁面
- [ ] 實現 AuthContext

### Phase 3: 活動管理
- [ ] 活動 CRUD API
- [ ] 報名功能
- [ ] 前端活動管理頁面

### Phase 4: 配對演算法 (核心功能)
- [ ] 配對分數計算
- [ ] 組合生成演算法
- [ ] 貪心選擇邏輯
- [ ] 單元測試

### Phase 5: 比賽管理
- [ ] 比賽狀態管理
- [ ] 場地分配邏輯
- [ ] 自動配對定時任務
- [ ] 前端比賽視圖

### Phase 6-8: 系統管理、測試與部署
- [ ] 管理後台
- [ ] 完整測試
- [ ] 部署配置

## 預計完成時間

2-3 個月（含開發、測試和優化）

## 核心功能概述

### 1. 用戶管理
- 用戶註冊與等級設定（1-10 級）
- 個人資料編輯
- 三種角色：球友、團主、管理員

### 2. 活動管理
- 團主創建活動（設定場地數、開始/結束時間）
- 球友報名參與
- 實時活動狀態查看

### 3. 智能配對
- 基於多因素的配對演算法：
  - **等級相近**（50% 權重）
  - **等待時間**（25% 權重）
  - **比賽次數均衡**（15% 權重）
  - **避免重複配對**（10% 權重）

### 4. 比賽流程
- 自動配對與場地分配
- 比賽狀態管理
- 輪詢機制實現上場通知

## 貢獻指南

目前為個人開發項目。如有建議或意見，歡迎提出 Issue。

## 許可證

MIT License

## 聯絡方式

有任何問題或建議，請通過以下方式聯絡：
- 提交 Issue：GitHub Issues
- 發送郵件：[your-email@example.com]
